# SQLite database (relative to prisma/)
DATABASE_URL="file:./dev.db"

# Session signing secret — change in production
AUTH_SECRET="replace-with-a-long-random-string"

# Admin mode password (demo)
ADMIN_PASSWORD="change-me"

# Public app name
NEXT_PUBLIC_APP_NAME="CampusOS — Adamas University"

# Google Maps is NOT required. Campus map uses Leaflet + OpenStreetMap only.
